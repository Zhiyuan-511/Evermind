# Evermind - AI Workflow Orchestrator

# Dependencies
node_modules/
__pycache__/
*.pyc
.next/
.venv/
venv/
env/

# Environment variables (API keys!)
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# v7.7 audit: archived dead-code snapshots not part of the runtime build
archive_v*_deadcode/
archive_*/
backup-pre-rebase

# Build & Output
dist/
build/
out/
/tmp/evermind_output/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Docker
docker-compose.override.yml

# Electron app binary artifacts (too large for GitHub)
# The whole .app bundle is a build output — it's rsync'd from backend/ and
# electron/dist/, so nothing inside it should be tracked. Keep only the source
# trees under version control.
Evermind.app/

# Claude config
.claude/
CLAUDE.md

# Task/build artifacts
test_deep_mode.py
codex_review_round4.md
HANDOFF_V5_UI.md
evermind_godmode_final.html
splash.html
start.command

# IDE/temp
*.orig
*.rej

# Generated python caches inside any subtree
**/__pycache__/
**/*.pyc

# Local release artifacts
release-check/
*.dmg
*.dmg.blockmap
latest-mac.yml

# Electron-builder local output
electron/dist/
electron/out/

# Next.js standalone build artifact (含个人路径，不该 push)
electron/.packaged/
